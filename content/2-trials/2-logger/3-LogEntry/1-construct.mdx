# LogEntry()


Initializes a LogEntry with the current timestamp, the ID of the thread that
created the log entry, the specified log level, and copies the message into the
message array.


## Parameters


### lvl

The log level (`INFO`, `WARNING`, `ERROR`) of the log entry.


### msg

The message to log.

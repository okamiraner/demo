# AlignedAllocator()

Initializes the allocator by allocating a memory pool of the specified size with
the specified alignment. Throws std::bad_alloc if the allocation fails.


## Parameters


### size

The total size of the memory pool to allocate.


### alignment

The alignment requirement for the allocated memory.
